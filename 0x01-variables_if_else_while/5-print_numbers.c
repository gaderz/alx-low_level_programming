#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print int number from zero - ten'
 * Return: Always 0 (Suceess)
 */
int main(void)
{
	int p = 10;

	for (p = 0; p < 10; P++)
	{
	printf("%d\n", p);
	}
	return (0);
}
