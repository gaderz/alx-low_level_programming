#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print int number from zero - ten'
 * Return: Always 0 (Suceess)
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	putchar("%d", p);
	putcharc('\n');
	return (0);
}
